## Codebase Patterns
- Pipeline step definitions live in `packages/core/src/types/pipeline.ts` (SOP_STEPS, SOP_STEP_NAMES)
- UI step names are duplicated in pipeline-step-nav.tsx (STEP_NAMES), pipeline-progress.tsx (SHORT_STEP_NAMES), and pipeline-view.tsx (STEP_TITLES) — all must stay in sync
- Static API routes for specific steps live at `apps/web/src/app/api/projects/[id]/stages/<N>/<action>/route.ts`
- The `[step]/run/route.ts` dynamic route handles all step run requests; export step is blocked there
- Worker export-html handler references step numbers — keep in sync with UI
- Step completion flow: pipeline-view.tsx calls API routes which update DB stages and project currentStage
- Optional steps (currently step 11 = Edit for Style) use `/stages/<N>/complete` endpoint
- Skippable steps (currently step 9 = Integrate Critiques) use `/stages/<N>/skip` endpoint
- Critiques flow: step 8 (Devil's Advocate) → critiques/select API → either step 9 (Integrate) or skip to step 12 (Export)
- Worker runner.ts maps step numbers to handler functions via a switch statement — must stay in sync with pipeline order
- Handler functions internally reference their own step number for DB stage updates, audit logs, and currentStage advancement

---

## 2026-03-12 - US-001
- Removed Final Polish (old step 9) and reordered pipeline from 13 to 12 steps
- New order: 1-5 unchanged, Fact-Check(6), Human Review(7), Devil's Advocate(8), Integrate Critiques(9), Style Guide(10), Edit for Style(11), Export(12)
- Files changed:
  - `packages/core/src/types/pipeline.ts` (already done in prior commit)
  - `apps/web/src/components/projects/pipeline-view.tsx` (full rewrite — step titles, phases, completion messages, switch cases, state vars, floating bar logic, prerequisite map)
  - `apps/web/src/components/projects/pipeline-step-nav.tsx` (STEP_NAMES, GROUPS)
  - `apps/web/src/components/projects/pipeline-progress.tsx` (SHORT_STEP_NAMES, length 13→12)
  - `apps/web/src/app/api/projects/route.ts` (create 12 stages instead of 13)
  - `apps/web/src/app/(app)/projects/[id]/page.tsx` (step range 13→12)
  - `apps/web/src/app/api/projects/[id]/stages/[step]/run/route.ts` (max 13→12, step 13→12 export guard)
  - `apps/web/src/app/api/projects/[id]/export/route.ts` (step 13→12 references)
  - `apps/web/src/app/api/projects/[id]/critiques/select/route.ts` (stage 11→8, step 12→9, step 13→12)
  - `apps/web/src/app/api/projects/[id]/stages/11/complete/route.ts` (NEW — replaces old stages/7/complete)
  - `apps/web/src/app/api/projects/[id]/stages/9/skip/route.ts` (NEW — replaces old stages/12/skip)
  - Deleted: `stages/7/complete/`, `stages/9/complete/`, `stages/12/skip/`
  - `apps/worker/src/routes/stages.ts` (max 13→12)
  - `apps/worker/src/jobs/handlers/export-html.ts` (step 13→12)
  - `apps/web/src/app/(app)/(main)/help/page.tsx` (13-step→12-step references)
- **Learnings for future iterations:**
  - pipeline-view.tsx has extensive hardcoded step number references in floating bar logic, optional step handling, and prerequisites — any step reorder requires careful audit
  - Static route directories (stages/7/, stages/9/, stages/12/) must be moved/recreated when step numbers change
  - The critiques/select route has complex skip logic that advances currentStage — must match new step flow
  - FinalStylePassStep component still exists as dead code (not imported) — can be cleaned up in US-002
---

## 2026-03-12 - US-002
- Updated worker handlers for new 12-step pipeline numbering
- Changes:
  - `apps/worker/src/jobs/handlers/fact-check.ts` — step 8→6 (all DB stage refs, audit log, producedByStep)
  - `apps/worker/src/jobs/handlers/devils-advocate.ts` — step 11→8 (stage running/awaiting_human, audit log, currentStage)
  - `apps/worker/src/jobs/handlers/integrate-critiques.ts` — step 12→9, advances to stage 10 instead of 13 (stage running/completed, audit log, producedByStep, currentStage)
  - `apps/worker/src/jobs/handlers/style-edit.ts` — step 7→11, advances to stage 12 instead of 8 (stage running/completed, audit log)
  - `apps/worker/src/jobs/runner.ts` — remapped switch cases: 6→factCheck, 8→devilsAdvocate, 9→integrateCritiques, 11→styleEdit; removed finalStylePass import
  - Deleted: `apps/worker/src/jobs/handlers/final-style-pass.ts`
- **Learnings for future iterations:**
  - Each handler has 3-5 places referencing its step number: stage set running, audit log stepNumber, producedByStep in versions, stage set completed, and currentStage advancement
  - Dead code remains: `packages/core/src/prompts/final-style.ts` (exports buildFinalStyleSystemPrompt/UserMessage), `apps/web/src/components/projects/steps/final-style-pass-step.tsx` — both can be cleaned up
  - The `integrate-critiques` handler had a "Step 10 output" user message that needed updating to reflect new numbering
---
